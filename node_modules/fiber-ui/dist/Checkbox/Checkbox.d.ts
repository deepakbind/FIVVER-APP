import { MergeElementProps } from '../utils';
import { GroupProps } from './Group';
import React, { FC } from 'react';
export declare type CheckboxProps = MergeElementProps<'input', {
    checked?: boolean;
    defaultChecked?: boolean;
    onChange?: (event: React.ChangeEvent<HTMLInputElement>) => void;
    style?: React.CSSProperties & object;
    disabled?: boolean;
    value?: string;
}>;
declare type CheckboxFC<P> = FC<P> & {
    Group?: FC<GroupProps>;
};
declare const Checkbox: CheckboxFC<CheckboxProps>;
export { Checkbox };

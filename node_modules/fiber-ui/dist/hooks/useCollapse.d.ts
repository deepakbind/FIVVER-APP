import React from 'react';
declare type CollapseProps = {
    children?: React.ReactNode | React.ReactNode[];
    collapsed?: boolean;
};
export declare const useCollapse: ({ children, collapsed }: CollapseProps) => {
    childrenNode: JSX.Element;
    height: number | undefined;
    collapseRef: React.MutableRefObject<Element | undefined>;
};
export {};

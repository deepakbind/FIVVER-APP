import { MergeElementProps } from '../utils';
import React, { FC } from 'react';
export declare type HeaderProps = MergeElementProps<'div', {
    style?: React.CSSProperties & object;
}>;
declare const Header: FC<HeaderProps>;
export { Header };

import { Locale } from './locales/LocaleProvider';
import { FC, ReactNode } from 'react';
interface UIProviderProps {
    children: ReactNode;
    theme?: Object;
    locale?: Locale;
}
export declare const UIProvider: FC<UIProviderProps>;
export {};

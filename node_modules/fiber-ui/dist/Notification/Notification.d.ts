import { MergeElementProps } from '../utils';
import React, { FC } from 'react';
export declare type NotificationProps = MergeElementProps<'div', {
    message?: string;
    description?: string;
    duration?: number;
    offset?: number;
    placement?: 'topRight' | 'topCenter' | 'topLeft' | 'bottomRight' | 'bottomCenter' | 'bottomLeft';
    destroy?: () => void;
    closeIcon?: React.ReactNode | boolean;
    style?: React.CSSProperties & object;
}>;
declare const Notification: FC<NotificationProps>;
declare const notification: {
    open: (args: NotificationProps) => void;
};
export { notification, Notification };
